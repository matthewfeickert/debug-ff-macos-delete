# debug-ff-macos-delete
delete me

> [!CAUTION]
> If running locally do not run the `git-init` task (required for CI) as this will overwrite your global Git user name and email.
> Instead for local tests run `pixi run start`.
